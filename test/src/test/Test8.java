package test;
/**

1.接口必须为public abstract，因为接口是高度抽象的类，它的存在就是被其他类实现。
2.接口中声明的变量必须为 public static final。
3.接口中定义的方法必须为public abstract，且没有方法体。
 * @author feiben
 *
 */
public abstract interface Test8 {
	public 	static final int SS =10;
	public abstract void ss();
}
